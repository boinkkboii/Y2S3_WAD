npm install react-native-vector-icons
    for icon purpose

npm install react-native-curved-bottom-bar
    the curved "+" button at the tab navigator

npm install react-native-svg
    the CurvedBottomBar library uses SVG to draw the curve

npx react-native run-android
    can show logs in terminal instead of android studio virtual machine

npx react-native link react-native-fs
npx react-native link react-native-zip-archive

npm install papaparse
